name: Phase 4 stack observation
on:
  push:
    branches: ['feature/inkubator-github-evidence-daemon-v0']
    paths: ['.github/workflows/phase4-stack-observation.yml']
permissions:
  contents: write
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo staged
