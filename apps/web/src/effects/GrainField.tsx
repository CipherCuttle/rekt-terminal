import{useEffect,useRef}from'react';
export type Quality='ULTRA'|'HIGH'|'LOW'|'TERMINAL';
const cfg={ULTRA:{scale:1,dpr:2,fps:24},HIGH:{scale:.7,dpr:1.5,fps:20},LOW:{scale:.45,dpr:1,fps:15},TERMINAL:{scale:.4,dpr:1,fps:0}} as const;
function rng(seed:number){let a=seed>>>0;return()=>{a|=0;a=(a+0x6D2B79F5)|0;let t=Math.imul(a^(a>>>15),1|a);t=(t+Math.imul(t^(t>>>7),61|t))^t;return((t^(t>>>14))>>>0)/4294967296}}
export function GrainField({quality,reduced}:{quality:Quality;reduced:boolean}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const cv=ref.current;if(!cv)return;const ctx=cv.getContext('2d');if(!ctx)return;let raf=0,last=0,hidden=false;const q=cfg[quality],rr=rng(7);const resize=()=>{const d=Math.min(devicePixelRatio||1,q.dpr);cv.width=Math.max(2,innerWidth*q.scale*d);cv.height=Math.max(2,innerHeight*q.scale*d)};const draw=(t:number)=>{const W=cv.width,H=cv.height,g=ctx.createLinearGradient(0,0,0,H);g.addColorStop(0,'#090612');g.addColorStop(.55,'#100925');g.addColorStop(1,'#160B32');ctx.fillStyle=g;ctx.fillRect(0,0,W,H);for(let i=0;i<4;i++){const x=W*(.13+.25*i)+Math.sin(t*.00017*(i+1))*W*.08,y=H*(.25+.17*i)+Math.cos(t*.00013*(i+1))*H*.1,rad=H*(.32+.07*i),rg=ctx.createRadialGradient(x,y,0,x,y,rad);rg.addColorStop(0,`rgba(122,92,255,${.11-i*.012})`);rg.addColorStop(.68,'rgba(74,58,140,.055)');rg.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=rg;ctx.fillRect(0,0,W,H)}ctx.globalAlpha=.11;ctx.fillStyle='#a281ff';for(let i=0;i<Math.min(1200,Math.floor(W*H/4000));i++){const x=rr()*W,y=rr()*H,s=rr()<.8?1:2;ctx.fillRect(x,y,s,s)}ctx.globalAlpha=1};const loop=(t:number)=>{raf=requestAnimationFrame(loop);if(hidden||reduced||q.fps===0)return;if(t-last<1000/q.fps)return;last=t;draw(t)};const vis=()=>{hidden=document.hidden};resize();draw(0);addEventListener('resize',resize);document.addEventListener('visibilitychange',vis);if(!reduced&&q.fps)raf=requestAnimationFrame(loop);return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize);document.removeEventListener('visibilitychange',vis)}},[quality,reduced]);return <canvas ref={ref} id="grain" aria-hidden="true"/>}
