# Lovable handoff

This codebase is architecture-frozen. Lovable may improve responsive spacing, typography calibration, semantic HTML, component extraction, visual polish and accessibility without changing source semantics.

Do not:
- replace Lightweight Charts with decorative charting;
- turn transfers into sales;
- invent wallet net worth;
- add persistent shaders beyond the ambient background;
- move signal/data computation into React components;
- remove fixture/live provenance states;
- add wallet signing/trading execution.

First task: install dependencies, run `npm run build`, fix only actual build/runtime issues, then render the preview. Preserve the current visual language: purple degraded ambient field + sharp acid-green CRT instrumentation.
