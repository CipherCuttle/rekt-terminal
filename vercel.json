{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "git": {
    "deploymentEnabled": {
      "*": false,
      "main": true
    }
  },
  "services": {
    "web": {
      "root": "apps/web/",
      "ignoreCommand": "case \"${VERCEL_PROJECT_PRODUCTION_URL:-}\" in *rekt-terminal-demo*) echo 'Skipping duplicate rekt-terminal-demo web service'; exit 0 ;; *) exit 1 ;; esac"
    },
    "api": {
      "root": "apps/api/",
      "entrypoint": "src/server.ts",
      "ignoreCommand": "case \"${VERCEL_PROJECT_PRODUCTION_URL:-}\" in *rekt-terminal-demo*) echo 'Skipping duplicate rekt-terminal-demo api service'; exit 0 ;; *) exit 1 ;; esac"
    }
  },
  "rewrites": [
    {
      "source": "/health",
      "destination": { "service": "api" }
    },
    {
      "source": "/v1/:path*",
      "destination": { "service": "api" }
    },
    {
      "source": "/(.*)",
      "destination": { "service": "web" }
    }
  ]
}
